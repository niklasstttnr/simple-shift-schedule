# Docker Compose (root docker-compose.yml).
# Copy to .env and change as needed:
#   cp .env.example .env

# Database (postgres service)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=postgres
